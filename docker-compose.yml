services:
  lanmao:
    build: .
    container_name: lanmao-articles
    ports:
      - "8080:80"
    volumes:
      - ./articles:/data/articles
    environment:
      - TZ=Asia/Shanghai
      - FETCH_INTERVAL=43200  # 12小时 = 43200秒
    restart: unless-stopped

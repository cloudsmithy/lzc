version: '3.8'

services:
  article-fetcher:
    build: .
    container_name: lanmao-articles
    ports:
      - "8080:80"
    volumes:
      - ./articles:/data/articles
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
